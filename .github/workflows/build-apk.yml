name: Android Build

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    name: Build Debug APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Generate debug keystore
        run: |
          keytool -genkey -v \
            -keystore debug.keystore \
            -alias androiddebugkey \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -storepass android \
            -keypass android \
            -dname "CN=Android Debug,O=Android,C=US"

      # ✅ جديد: توليد gradle wrapper إذا لم يكن موجودًا
      - name: Setup Gradle and generate wrapper if missing
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: 8.5   # أو أي إصدار يدعم مشروعك
      
      - name: Generate Gradle Wrapper if needed
        run: |
          if [ ! -f "./gradlew" ]; then
            echo "gradlew not found, generating..."
            gradle wrapper --gradle-version 8.5
          else
            echo "gradlew exists, skipping generation."
          fi

      - name: Build debug APK
        run: ./gradlew assembleDebug   # الآن gradlew موجود بالتأكيد

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: app/build/outputs/apk/debug/app-debug.apk
